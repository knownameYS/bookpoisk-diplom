version: '3.9'
services:
  postgres:
    image: postgres:16-alpine
    container_name: bookpoisk-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: bookpoisk
      POSTGRES_PASSWORD: bookpoisk
      POSTGRES_DB: bookpoisk
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
